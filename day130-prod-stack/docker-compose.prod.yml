services:
  api:
    ports:
      - "80:80"
    environment:
      - APP_ENV=production
      - LOG_LEVEL=error
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 128M
