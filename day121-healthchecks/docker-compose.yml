services:
  web-app:
    image: nginx:alpine
    container_name: monitored-web
    ports:
      - "8087:80"
    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost/"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    restart: always
